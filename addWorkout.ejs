<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Add Workout | Fitness Tracker</title>
    <link rel="stylesheet" href="/style.css">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;600&display=swap" rel="stylesheet">
    <style>
        /* 🌟 Global Styles */
        body {
            font-family: "Poppins", sans-serif;
            background-color: #121212;
            color: white;
            margin: 0;
            padding: 0;
            text-align: center;
            display: flex;
            flex-direction: column;
            min-height: 100vh;
        }

        /* 🌟 Header */
        header {
            background: linear-gradient(90deg, #007bff, #0056b3);
            color: white;
            padding: 25px;
            font-size: 26px;
            text-transform: uppercase;
            letter-spacing: 2px;
            font-weight: bold;
            box-shadow: 0 4px 10px rgba(0, 123, 255, 0.4);
        }

        header h1 {
            margin: 0;
        }

        /* 🌟 Buttons */
        .btn {
            display: inline-block;
            padding: 12px 25px;
            text-decoration: none;
            color: white;
            border-radius: 30px;
            font-weight: bold;
            transition: 0.3s;
            box-shadow: 0px 5px 10px rgba(0, 123, 255, 0.3);
            font-size: 18px;
        }

        .btn.primary { background-color: #007bff; }
        .btn.workout { background-color: #ff5733; }

        .btn:hover {
            opacity: 0.9;
            transform: scale(1.05);
        }

        /* 🌟 Container */
        .form-container {
            max-width: 500px;
            margin: 40px auto;
            background: #1c1c1c;
            padding: 25px;
            border-radius: 12px;
            box-shadow: 0px 5px 15px rgba(0, 123, 255, 0.3);
            border: 1px solid rgba(0, 123, 255, 0.3);
            flex: 1;
        }

        h2 {
            color: #007bff;
            font-size: 24px;
            text-transform: uppercase;
            font-weight: bold;
            margin-bottom: 20px;
        }

        /* 🌟 Form Styling */
        form {
            display: flex;
            flex-direction: column;
            gap: 15px;
        }

        input {
            width: 100%;
            padding: 12px;
            font-size: 16px;
            border-radius: 8px;
            border: none;
            outline: none;
            background: #2a2a2a;
            color: white;
            transition: 0.3s;
        }

        input:focus {
            border: 2px solid #007bff;
            box-shadow: 0px 0px 10px rgba(0, 123, 255, 0.5);
        }

        /* 🌟 Submit Button */
        button {
            width: 100%;
            padding: 12px;
            font-size: 18px;
            font-weight: bold;
            border: none;
            border-radius: 30px;
            color: white;
            background-color: #ff5733;
            cursor: pointer;
            transition: 0.3s ease-in-out;
            box-shadow: 0 5px 10px rgba(255, 87, 51, 0.3);
        }

        button:hover {
            opacity: 0.9;
            transform: scale(1.05);
        }

        /* 🌟 Error Message */
        .error-message {
            color: #ff5733;
            font-weight: bold;
            margin-bottom: 15px;
            font-size: 16px;
        }

        /* 🌟 Back Link */
        .back-link {
            display: inline-block;
            margin-top: 15px;
            color: #20c997;
            font-size: 16px;
            text-decoration: none;
            transition: 0.3s;
        }

        .back-link:hover {
            color: #17a589;
            text-decoration: underline;
        }

        /* 🌟 Footer */
        footer {
            background: #222;
            color: white;
            padding: 20px;
            font-size: 14px;
            text-transform: uppercase;
            letter-spacing: 1px;
            margin-top: auto;
        }
    </style>
</head>
<body>

    <header>
        <h1>🏋️‍♂️ ADD WORKOUT</h1>
        <a href="/workouts" class="btn primary">🔙 Back to Workouts</a>
    </header>

    <div class="form-container">
        <h2>Add Workout 💪</h2>

        <!-- ✅ Error Handling -->
        <% if (typeof error !== "undefined" && error) { %>
            <p class="error-message"><%= error %></p>
        <% } %>

        <form action="/workouts/add" method="POST">
            <input type="text" name="name" placeholder="Workout Name (e.g., Push-ups)" required>
            <input type="number" name="duration" placeholder="Duration (minutes)" required>
            <input type="text" name="type" placeholder="Type (e.g., Cardio, Strength)" required>
            <button type="submit" class="btn workout">➕ Add Workout</button>
        </form>

        <a href="/workouts" class="back-link">🔙 Back to Workouts</a>
    </div>

    <footer>
        <p>Fitness Tracker | Stay Strong 💪</p>
    </footer>

</body>
</html>
