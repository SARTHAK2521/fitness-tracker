<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Add New Diet | Fitness Tracker</title>
    <link rel="stylesheet" href="/style.css">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;600&display=swap" rel="stylesheet">
    <style>
        /* 🌟 Global Styles */
        body {
            font-family: "Poppins", sans-serif;
            background-color: #121212;
            color: white;
            margin: 0;
            padding: 0;
            text-align: center;
            display: flex;
            flex-direction: column;
            min-height: 100vh;
        }

        /* 🌟 Header */
        header {
            background: linear-gradient(90deg, #007bff, #0056b3);
            color: white;
            padding: 25px;
            font-size: 26px;
            text-transform: uppercase;
            letter-spacing: 2px;
            font-weight: bold;
            box-shadow: 0 4px 10px rgba(0, 123, 255, 0.4);
        }

        header h1 {
            margin: 0;
        }

        /* 🌟 Buttons */
        .btn {
            display: inline-block;
            padding: 12px 25px;
            text-decoration: none;
            color: white;
            border-radius: 30px;
            font-weight: bold;
            transition: 0.3s;
            box-shadow: 0px 5px 10px rgba(0, 123, 255, 0.3);
            font-size: 18px;
        }

        .btn.primary { background-color: #007bff; }
        .btn.diet { background-color: #20c997; }

        .btn:hover {
            opacity: 0.9;
            transform: scale(1.05);
        }

        /* 🌟 Container */
        .container {
            max-width: 500px;
            margin: 40px auto;
            background: #1c1c1c;
            padding: 25px;
            border-radius: 12px;
            box-shadow: 0px 5px 15px rgba(0, 123, 255, 0.3);
            border: 1px solid rgba(0, 123, 255, 0.3);
            flex: 1;
        }

        h2 {
            color: #007bff;
            font-size: 24px;
            text-transform: uppercase;
            font-weight: bold;
            margin-bottom: 20px;
        }

        /* 🌟 Form Styling */
        form {
            display: flex;
            flex-direction: column;
            gap: 15px;
        }

        label {
            font-size: 16px;
            font-weight: bold;
            color: #ddd;
            text-align: left;
            display: block;
            margin-bottom: 5px;
        }

        input {
            width: 100%;
            padding: 12px;
            font-size: 16px;
            border-radius: 8px;
            border: none;
            outline: none;
            background: #2a2a2a;
            color: white;
            transition: 0.3s;
        }

        input:focus {
            border: 2px solid #007bff;
            box-shadow: 0px 0px 10px rgba(0, 123, 255, 0.5);
        }

        /* 🌟 Submit Button */
        button {
            width: 100%;
            padding: 12px;
            font-size: 18px;
            font-weight: bold;
            border: none;
            border-radius: 30px;
            color: white;
            background-color: #20c997;
            cursor: pointer;
            transition: 0.3s ease-in-out;
            box-shadow: 0 5px 10px rgba(32, 201, 151, 0.3);
        }

        button:hover {
            opacity: 0.9;
            transform: scale(1.05);
        }

        /* 🌟 Footer */
        footer {
            background: #222;
            color: white;
            padding: 20px;
            font-size: 14px;
            text-transform: uppercase;
            letter-spacing: 1px;
            margin-top: auto;
        }
    </style>
</head>
<body>

    <header>
        <h1>🥗 ADD NEW DIET PLAN</h1>
        <a href="/diet" class="btn primary">🔙 Back to Diet List</a>
    </header>

    <div class="container">
        <h2>Enter Diet Details 🍎</h2>

        <form action="/diet/add" method="POST">
            <label for="name">Diet Name:</label>
            <input type="text" id="name" name="name" placeholder="E.g., Keto Diet" required>

            <label for="calories">Calories (kcal):</label>
            <input type="number" id="calories" name="calories" placeholder="E.g., 1500" required>

            <button type="submit" class="btn diet">➕ Add Diet</button>
        </form>
    </div>

    <footer>
        <p>Fitness Tracker | Stay Healthy 💪</p>
    </footer>

</body>
</html>
